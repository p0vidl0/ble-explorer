{
  "name": "devices",
  "private": true,
  "version": "0.1.0",
  "description": "Standalone BLE supported devices page",
  "scripts": {
    "start": "parcel src/index.html",
    "build": "parcel build src/index.html",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "@parcel/transformer-webmanifest": "^2.12.0",
    "parcel": "^2.12.0",
    "vitest": "^3.2.4"
  }
}
